# Base Python dependencies — installed when ENABLE_PYTHON=true or ENABLE_FULL_SKILLS=true.
# Uses compatible release (~=) to allow patch updates within the same minor version.
# e.g. ~=7.2.8 allows >=7.2.8, <7.3.0
#
# Last reviewed: 2026-04-03

edge-tts~=7.2.8

# knowledge_extract (internal/tekshot/knowledge_extract.py) - deterministic file
# extraction for tekshot. pypdfium2 renders scanned pages without poppler.
# markitdown is NOT used: its magika->onnxruntime dependency has no musl wheel.
pdfplumber~=0.11.10
pypdfium2~=5.13.0
openpyxl~=3.1.5
mammoth~=1.12.1
markdownify~=1.2.3
python-pptx~=1.0.2
